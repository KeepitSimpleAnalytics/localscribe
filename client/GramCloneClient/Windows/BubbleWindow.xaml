<Window x:Class="GramCloneClient.Windows.BubbleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LocalScribe Bubble" Height="26" Width="26"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" Focusable="False">
    <Grid>
        <Border Name="BubbleBorder" CornerRadius="13" Background="#B24CAF50" BorderBrush="White" BorderThickness="1"
                MouseLeftButtonDown="BubbleBorder_MouseLeftButtonDown" Cursor="Hand" Opacity="0.8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.3"/>
            </Border.Effect>
            <TextBlock Name="CountText" Text="✓" HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Foreground="White" FontWeight="Bold" FontSize="12"/>
        </Border>

        <Popup Name="ErrorPopup" PlacementTarget="{Binding ElementName=BubbleBorder}" 
               Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
            <Border Background="White" CornerRadius="8" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10" Width="350">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="4" Opacity="0.2"/>
                </Border.Effect>
                <ScrollViewer MaxHeight="500" VerticalScrollBarVisibility="Auto">
                    <ItemsControl Name="ErrorsList"/>
                </ScrollViewer>
            </Border>
        </Popup>
    </Grid>
</Window>
